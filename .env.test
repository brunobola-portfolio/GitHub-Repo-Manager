# Test-mode overrides for e2e runs (loaded via `vite --mode test`).
# Takes precedence over .env for VITE_* variables only when vite is invoked
# with --mode=test, so developer local config stays untouched.
VITE_MOCK_MODE=true
VITE_API_BASE_URL=
