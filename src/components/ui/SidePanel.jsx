// @deprecated — use <Drawer> from './Drawer.jsx' instead.
// Kept as a re-export shim so any external imports keep working.
// All in-tree consumers were migrated to <Drawer> in the drawer-consolidation slice.
export { Drawer as SidePanel } from './Drawer.jsx'
