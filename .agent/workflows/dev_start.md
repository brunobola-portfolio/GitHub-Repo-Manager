---
description: Start the full development environment (frontend + backend)
---

1. Ensure we are in the project root directory.

// turbo
2. Run `cmd /c npm run dev:all`

3. Wait for both servers to start:
   - Frontend: http://localhost:5173
   - Backend API: http://localhost:3001

4. Confirm both servers are running and ready for development.

