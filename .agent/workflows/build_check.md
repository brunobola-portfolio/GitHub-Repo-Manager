---
description: Run build and verify no errors before committing
---

1. Run the production build.
// turbo
2. Run `cmd /c npm run build`

3. Check for any build errors or warnings.

4. If build succeeds, report ready for commit.

5. If build fails, analyze errors and suggest fixes.

